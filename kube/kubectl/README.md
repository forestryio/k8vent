# Deploying k8vent with `kubectl`

This directory contains the resources needed to install k8vent using
the `kubectl` command-line utility.  See [Running][run] for detailed
instructions.

[run]: ../../README.md#running
