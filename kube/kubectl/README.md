# Deploying k8svent with `kubectl`

This directory contains the resources needed to install k8svent using
the `kubectl` command-line utility.  See [Running][run] for detailed
instructions.

[run]: ../../README.md#running
